# Smooth coverage by window width (smooth_coverage_by_window_v2.pl)
This script allows to create smooth coverage plots.

Usage: $0 <TAB_file> <window>

	<TAB_file>	= TAB file to be smoothed
	<window>	= window width
  
 # AUTHOR: Ramón Peiró-Pastor
  
